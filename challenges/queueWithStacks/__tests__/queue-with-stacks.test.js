'use strict'

const stacks = require('../queue-with-stacks.js');

describe('queue with stacks', () => {
  let myQueue  = new PsudoQueue;

  it('will enqueue one item', () => {
    
  });
});