'use strict';

class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class Hashtable{

  constructor(item){
    this.item = item;
    this.table = [];
  }

  add(key,value){
    
  }

}